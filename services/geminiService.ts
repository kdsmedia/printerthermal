
/**
 * Gemini AI Service removed by user request.
 * All extraction features are now disabled.
 */
export const extractShippingData = async (base64Image: string) => {
  return {};
};

export const extractReceiptData = async (base64Image: string) => {
  return [];
};
